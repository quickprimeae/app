// src/lib/config.ts
// Pilot is single-tenant. This is the founding company's tenant id
// (QuickPrime Internal). When the platform goes multi-tenant this should
// move to a per-request lookup (e.g. by subdomain or the ops user's tenant).
export const PILOT_TENANT_ID = '678151ad-d086-4164-b187-c2804d21cb54'
